# Data

This directory is reserved for local prompt corpora, generated paraphrases, and benchmark
fixtures that are too large or too environment-specific to commit.

The first implementation does not ship training data. It ships a cosine baseline and
customer-support fixtures so future classifier work can measure improvement against a
known failure mode.
